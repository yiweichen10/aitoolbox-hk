# 2026-08-05 工作日志

## 🔴 价格字段溢出问题（用户反馈：topaz-photo-ai 页面红框位置）

### 问题
- 用户截图：https://www.aitoolbox.hk/tools/topaz-photo-ai/
- 两处溢出：
  1. Hero 元信息（tool-meta-item）：💰 Price 一条占满 5 行挤压 Platform 列
  2. Key Statistics 卡片：Pricing 单卡 276 字符文本，撑爆 grid 整块区域

### 根因（三层）
1. **数据层**：`tools_en.json:9559` Topaz Photo AI 的 `price` 字段塞了 276 字符长段落，不是价格标签
2. **渲染层**：`build_en.py:587, 662` 两处直接 `{tool["price"]}` 渲染，无截断
3. **CSS 层**：`style.css:1584` `.ks-val` 是 20px 粗体大字，无 line-clamp / overflow

### 同类污染（已扫描，len(price) > 250）
| 工具 | 字符数 |
|---|---|
| Exa AI | 750 |
| Elicit | 670 |
| LanguageTool | 503 |
| Voiceflow | 467 |
| Wondercraft AI | 327 |
| Murf AI | 320 |
| Cleanvoice | 278 |
| AIVA | 277 |
| **Topaz Photo AI** | **276** |
| Soundraw | 275 |

### 优化方案（A/B/C 三档）
- **A. 立即修复（CSS + build 截断）**：加 `.ks-val.is-long` 类，长文降级到 13px/clamp 5行；build_en.py 加 len>60 截断 + title hover
- **B. 治本（数据治理）**：加 `pricing_details` 字段，`price` 改短标签（"<50 字符"），长详情移入 pricing_details
- **C. 长期防御**：build 校验 + 迁移脚本源头拆分

### ⛔ 用户决策（2026-08-05 16:51）：禁止治标
- 用户原话：「方案A不行，靠截断不解决根本问题。以后禁止用治标的方案。」
- 方案 A（CSS 截断 / build 截断）被否。走方案 B + C 纯治本。

### ✅ 已执行（治本完成）
1. **数据重塑**（`scripts/fix_price_labels.py`）：23 个 len(price)>80 的工具，AI 语义化重写为短标签（≤29 字符），原长文整体移入 `pricing_details` 字段。全库校验通过：无 price>80 残留。
2. **渲染自愈**：build_en.py 本就直接读 `price` 字段，数据治本后卡片自然正确，无需改渲染逻辑（证明根因在数据）。
3. **长期防御**：
   - `build_en.py`：构建时校验 price>80 打印 `[PRICE WARNING]`（报警，非截断）
   - `gen_tools_en.py`：迁移时若 price>80 自动拆分短标签 + pricing_details（结构化拆分，非截断）
4. **重建验证**：`build_en.py --target tools` 成功，无任何 [PRICE WARNING]；topaz 页 Hero Price = "From $199 one-time"，Key Statistics Pricing 卡 = "From $199 one-time"，无溢出。

### 文件变更
- `data/tools_en.json`（已备份 `data/tools_en.json.20260805.bak`）：23 个工具 price 改短标签 + pricing_details
- `scripts/fix_price_labels.py`（新增）：治理脚本，SHORT_MAP 可复用
- `scripts/build_en.py`：+price 校验（第 1668 行后）
- `scripts/gen_tools_en.py`：+price 拆分（import re + 价格治理块）
- `tools/topaz-photo-ai/index.html` 等 153 个工具页已重建

### 记忆
- 用户级 `~/.workbuddy/MEMORY.md`：新增「禁止治标，必须治本」跨项目原则
- 项目级 `seo-site-en/.workbuddy/memory/MEMORY.md`（新建）：价格字段规范 + 禁止治标 + 录入脚本污染源标注

### 🧩 Skill 沉淀（2026-08-05 17:55）
- 新建 `~/.workbuddy/skills/seo-data-field-governance/SKILL.md`（agent_created: true）
- 把「数据字段治本 SOP + 构建期 WARNING 校验防复发」固化成可复用执行手册：根因定位→数据备份→超长字段扫描→AI 语义化重写(禁截断)→字段拆分→构建校验→源头治理→重建验证
- 触发：价格溢出/字段污染/显示异常但根因在数据/禁止治标 类问题

### 🔍 SEO 站点审计 + 意图词挖掘（2026-08-05 20:xx）
- 英文站 aitoolbox.hk 技术审计结论：schema 覆盖满分(工具153/153+文章120/120 JSON-LD)、内链0死链、hreflang+canonical规范、AI爬虫白名单已布；待修 P0: Jasper两页标题重复(cannibalization) + 2个孤儿文章目录未入data；P1: 38张>200KB图未WebP(LCP隐患)、文章页缺FAQ schema、about/author未确认入sitemap
- **核心关键词策略共识**：小站不盯 GSC(空表无意义)，改用站外词源——竞品排名词(Ahrefs免费版)、Google自动补全、你153工具×30分类资产交叉生成；用户明确"没流量哪来的意图词"误区已纠正(搜索量在词本身不在你的站)
- **产出意图词候选表**：`scripts/gen_keyword_opportunities.py` → `exports/keyword_opportunities.csv/.md`，628词(对比99/品牌446/长尾83)，430 GAP，202 P1未覆盖；已清脏词(ai ai重复/Jasper自比)；三类：Comparison(同类互比)、Brand(153工具review/alternatives/vs竞品)、Long-tail(分类×场景)
- 用户决策：不依赖GSC/Reddit，纯站点资产驱动找词방향确认可行
