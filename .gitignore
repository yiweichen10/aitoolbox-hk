# Environment variables — never commit secrets
.env
.env.*
!.env.example

# DataForSEO keyword data — local only, never commit
data/_kw/

# Python cache
__pycache__/
*.py[cod]
*.pyo

# Temp data scripts — never commit
data/_*.py
# Backup files — never commit
*.bak

# Content exports — generated, never commit
exports/

# OS
.DS_Store
Thumbs.db
